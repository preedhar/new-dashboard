import { AdminPagePlaceholder } from './AdminPagePlaceholder'

export function AdminSettingsPage() {
  return (
    <AdminPagePlaceholder
      title="Settings"
      description="A future workspace for store preferences, payments, shipping, and account settings."
    />
  )
}
