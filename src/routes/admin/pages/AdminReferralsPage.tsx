// The referral programme's page, reached from the sidebar's "Refer & Earn $500"
// card. Empty until the programme is designed — the admin shell still supplies
// the breadcrumb and title around it.
export function AdminReferralsPage() {
  return null
}
